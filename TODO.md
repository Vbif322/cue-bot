# TODO

Приоритеты по аудиту от 2026-06-12 (`audit/FINDINGS.md`, сводка —
`audit/stage-8-synthesis.md`)

## M2 — Виды бильярда и дисциплины

Разбивка [ROADMAP.md](ROADMAP.md) на задачи.

#### M2-7 техдолг (полировка снукерного ввода — после ширины M2)

- **P3 — UX захвата макс. брейка (кандидат подтянуть раньше):** сейчас промпт-кнопка на каждый
  фрейм; финальный (решающий) фрейм авто-завершается и брейк по нему не записать — дыра в данных.
  Вынести в один промпт «макс. брейк за матч» в конце ввода (снукер-онли); хранение по-фреймовое.
  Стоит сделать раньше, если организаторам снукера реально важен макс. брейк.
- **P2 — редактирование результата:** сейчас undo только последнего фрейма; при dispute репортёр
  вводит все фреймы заново. Добавить правку отдельного фрейма / повторный вход в незавершённый ввод.
- **P3 — сосуществование текстовых стейтов:** ввод фреймов и `matchScheduleState` делят один
  `message:text`-хендлер; при добавлении новых текстовых флоу вынести в общий диспетчер сессий.

## M7 — бот в групповом чате

Этап 1 (анонсы об открытии регистрации) сделан, см. [ROADMAP.md](ROADMAP.md) M7.
Остаток этапа, по приоритету:

- **P1 — привязка анонсов к турнирам.** Сейчас чат подписан на все публичные турниры:
  `resolveAnnouncementTargets` (`src/services/groupChatService.ts`) возвращает все
  активные чаты. Привязка (junction `tournament_chats` либо `federationId` из M8) —
  это новое тело того же шва; `group_chats` и `group_announcements` менять не нужно.
  Тогда же решается, остаётся ли автоподписка при добавлении бота (дефолт
  `group_chats.isActive`) или чат включается только явным `/start_announcements`.
- **P2 — остальные события в группу** поверх готового `postToGroupChat`
  (`src/services/groupBroadcastService.ts`): закрытие регистрации, старт турнира,
  результаты матчей, финальная сетка. Добавить значения в `groupAnnouncementKinds`
  (`src/db/schema/groupAnnouncements.ts`) — уникальный индекс по (чат, турнир, вид)
  уже разводит их между собой.
- **P2 — редактирование анонса на месте.** `group_announcements.messageId` пишется,
  но нигде не читается: обновлять счётчик участников в уже отправленном сообщении.
- **P3 — миграция группы в супергруппу** переносится на новый `chat_id` только на
  стороне `group_chats`; строки лога остаются на старом id (`// TODO M7` в
  `groupBroadcastService.ts`). Худший исход сегодня — один дубль анонса.
- **P3 — управление турниром из группы** (регистрация, результаты, сетка). Потребует
  расширить белый список в `chatScopeMiddleware` (`src/bot/middleware/chatScope.ts`),
  который сейчас пропускает в группе только `my_chat_member` и две команды анонсов,
  и продумать права: сегодня `callback_query` из группы отбрасывается намеренно.

## Ближайший месяц — фоновой планировщик джобов

Следующий приоритет сразу после M2 по [ROADMAP.md](ROADMAP.md) (раздел «Доработки турнирного
ядра»). Задачи упорядочены по приоритету: сначала дешёвые независимые quick win'ы, затем
фундамент (сам планировщик), затем его самый ценный потребитель, затем остальное.

- **P0 — quick wins**, без зависимостей от планировщика, можно делать в любой момент:
  - колонка `endDate` в `tournaments` (сейчас только `startDate`) + миграция, поле в визарде
    создания и админ-SPA, отображение диапазона дат;
  - рендер `scheduledAt` в игровом вебе — данные уже есть в типах (`app/src/lib/types.ts`), но
    не выводятся в `Bracket`/`MatchModal` (`app/`).
- **P1 — базовый сервис планировщика**, по образцу `dialogSessionSweep` (`src/index.ts`):
  фундамент, без которого не завести ни один из следующих пунктов.
- **P1 — авто-подтверждение результата по таймауту**: первый и самый ценный потребитель
  планировщика — если оппонент не реагирует на заявленный результат за отведённое время,
  автоматически подтверждать счёт заявителя и уведомлять. В ROADMAP.md прямо помечено как
  кандидат вытащить уже в M2, «если матчи подвисают на реальных турнирах». Логика была в
  удалённом `timeoutService`; строить поверх confirm-флоу M2-7.
  Сделать до или вместе с этим пунктом — **P2 «редактирование результата»** из блока M2-7
  техдолга выше (`matchService.ts`): сейчас при dispute репортёр вводит все фреймы заново;
  как только подтверждение начнёт происходить автоматически, а не по явному клику оппонента,
  споры постфактум станут чаще, а без нормальной правки результата единственный выход из
  спора — полный re-ввод.
- **P2 — остальные потребители планировщика**, следующим спринтом:
  - напоминания о матче за N часов до `matches.scheduledAt` обоим игрокам (тип
    `match_reminder` уже есть в `src/db/schema/notifications.ts`, сейчас шлётся один раз при
    назначении времени через `notifyMatchScheduled`);
  - дедлайны матчей — по истечении срока на сыгранную встречу техпоражение через
    `setTechnicalResult` (`src/services/matchService.ts`) и уведомление.
